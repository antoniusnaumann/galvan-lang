[package]
name = "galvan-serde-json"
version = "0.1.0"
edition = "2024"

[dependencies]
galvan = { path = "../.." }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
galvan = { features = ["build"], path = "../.." }

[workspace]
