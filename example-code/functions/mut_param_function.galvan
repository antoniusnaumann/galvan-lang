/*# AST
single(
    function(
        public(),
        "bark",
        params(
            vec![
                (DeclModifier::Mut, "dog", plain("Dog")),
            ]),
        None,
        body()
    )
)
*/

/*# TRANSPILE
"pub fn bark(dog: &mut Dog) { }"
*/

pub fn bark(mut dog: Dog) { }