/*# AST
single(
    function(
        public(),
        "add",
        params(
            vec![
                ("a", plain("Int")),
                ("b", plain("Int"))
            ]),
        Some(plain("Int")),
        body()
    )
)
*/

/*# TRANSPILE
"pub fn add(a: Int, b: Int) -> Int { }"
*/

pub fn add(a: Int, b: Int) -> Int {
    // a + b
}