# Functions

Functions are the only place code lives in Galvan — there are no methods
inside type declarations and no `impl` blocks. A function whose first
parameter is named `self` *is* a method, no matter where it is declared. This
chapter covers declarations, member functions, extending types you don't own,
overloading via argument labels, and Galvan's lightweight call syntax.
