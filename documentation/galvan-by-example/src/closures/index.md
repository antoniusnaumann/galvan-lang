# Closures

Closures are anonymous functions that capture their environment. Galvan
writes them with a pipe-delimited parameter list — the same shape appears in
closure *types*, and the trailing-closure call syntax makes higher-order code
read like control flow.
